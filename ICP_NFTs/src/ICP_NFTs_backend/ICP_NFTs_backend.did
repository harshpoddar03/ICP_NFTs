type Result = variant { Ok; Err : text };
service : {
  add_authorized_minter : (principal) -> (Result);
  burn_nft : (nat64) -> (bool);
  get_token_content : (nat64) -> (opt text) query;
  mint_nft : (principal, text) -> (nat64, text);
  transfer_nft : (principal, nat64) -> (bool);
}