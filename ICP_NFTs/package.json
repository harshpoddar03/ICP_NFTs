{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "ICP_NFTs",
  "scripts": {
    "build": "dfx generate && cd src/ICP_NFTs_frontend && npm run build",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/ICP_NFTs_frontend"
  ],
  "dependencies": {
    "@dfinity/agent": "^2.0.0",
    "@dfinity/auth-client": "^2.0.0"
  }
}
